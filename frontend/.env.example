# API endpoints
# When accessing via nginx (localhost:8082): Leave empty to use window.location.origin
# When accessing frontend directly (localhost:5175): VITE_API_BASE_URL=http://localhost:3002
# Production (same domain): Leave empty to use window.location.origin
VITE_API_BASE_URL=
VITE_API_VERSION=/api/v1
VITE_GOOGLE_AUTH_PATH=/api/v1/oauth/google
VITE_FACEBOOK_AUTH_PATH=/api/v1/oauth/facebook
VITE_GOOGLE_CALLBACK_PATH=/api/v1/oauth/google/callback
VITE_FACEBOOK_CALLBACK_PATH=/api/v1/oauth/facebook/callback

# Frontend hostname allowlist for Vite dev server (comma-separated)
# Development: Add all domains you'll access the frontend from
# Example: VITE_ALLOWED_HOSTS=dev.charhub.app,staging.charhub.app
# Default (if empty): localhost,127.0.0.1,dev.charhub.app
# Production: VITE_ALLOWED_HOSTS=charhub.app
VITE_ALLOWED_HOSTS=dev.charhub.app

# HMR (Hot Module Replacement) configuration for Vite dev server
# The default configuration uses port 5173 mapped in docker-compose.yml
# This allows the browser to connect directly to Vite for WebSocket HMR, bypassing nginx
#
# Override these only if you need different settings:
# VITE_HMR_HOST=localhost (default: localhost)
# VITE_HMR_PROTOCOL=ws (default: ws, use wss for HTTPS)
# VITE_HMR_CLIENT_PORT=5173 (default: 5173, must match docker-compose port mapping)
#
# Leave commented for default behavior
# VITE_HMR_HOST=
# VITE_HMR_PROTOCOL=
# VITE_HMR_CLIENT_PORT=

# CDN / Assets
# Development: VITE_CDN_PUBLIC_URL_BASE=https://dev.charhub.app
# Production: VITE_CDN_PUBLIC_URL_BASE=https://charhub.app
VITE_CDN_PUBLIC_URL_BASE=

# Feature flags
# Set to 'true' to use mock data for characters (useful during development)
# Set to 'false' or leave empty to use real API
VITE_USE_CHARACTER_MOCKS=false

# ============================================
# Stripe Payment Configuration
# ============================================
# Documentation: https://stripe.com/docs/keys
#
# TEST MODE (Development):
#   - Get from: https://dashboard.stripe.com/test/apikeys
#   - Publishable Key: pk_test_...
#
# PRODUCTION MODE:
#   - Get from: https://dashboard.stripe.com/apikeys
#   - Publishable Key: pk_live_...
#
# IMPORTANT:
#   - This is the PUBLISHABLE key (safe to expose in frontend)
#   - Never use the SECRET key in frontend code!
#   - Must match the environment of backend's STRIPE_SECRET_KEY

VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
